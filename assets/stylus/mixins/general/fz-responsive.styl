/*
	fz-responsive - ver. 1.0.0
	-----
	Миксин для ресайза размера шрифта по заданным 3 точкам.
	Принимает в себя параметры, которые берутся из дефолтных переменных
	Параметры:
		1. $fzMin - минимальный размер шрифта
		2. $fzMid - стандартный размер шрифта
		3. $fzMax - максимальный размер шрифта
		4. $widthMin - ширина при которой будет у сайта минимальный размер шрифта
		5. $widthMid - ширина при которой будет у сайта стандартный размер шрифта
		6. $widthMax - ширина при которой будет у сайта максимальный размер шрифта
		7. $min (boolean) - если включен, то после минимальной ширины экорана, шрифт не будет больше уменьшаться
*/

fz-responsive($fzMin, $fzMid, $fzMax, $widthMin, $widthMid, $widthMax, $min)
    @media (min-width $widthMid)
        font-size: s("calc(%s + (100vw - %s) * %s)", $fzMid, $widthMid, round(unit(($fzMax - $fzMid) / ($widthMax - $widthMid), ""), 6))
  
    @media (min-width $widthMin) and (max-width $widthMid)
        font-size: s("calc(%s + (100vw - %s) * %s)", $fzMin, $widthMin, round(unit(($fzMid - $fzMin) / ($widthMid - $widthMin), ""), 6))

    if $min == true
        @media (max-width $widthMin)
            font-size: $fzMin